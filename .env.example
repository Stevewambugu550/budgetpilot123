# GEMINI_API_KEY: Optional. Enables real AI-generated advice from the AI Savings
# Advisor (via the Express dev server locally, or the ai-advisor Netlify Function
# in production). Without it, the advisor falls back to local rules-based insights.
GEMINI_API_KEY="your-gemini-api-key-here"
